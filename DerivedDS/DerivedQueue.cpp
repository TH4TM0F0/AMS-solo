#include "DerivedQueue.h"
#include <iostream>
using namespace std;

template<typename T>
void DerivedQueue<T>::search(T& entry)
{
	//Node<T>* iteratorptr = this->frontPtr;

	/*while (iteratorptr)
	{
		if Car c1(1, "SC", 100, Ready, 1,nullptr);
	Car c2(2, "SC", 100, Ready, 1, nullptr);
	Car c3(3, "SC", 100, Ready, 1, nullptr);
	LinkedQueue<Car> queue;
	queue.enqueue(c1);
	queue.enqueue(c2);
	queue.enqueue(c3);
	}*/


}
template<typename T>
void DerivedQueue<T>::print()
{
	//Node<T>* iteratorptr = new Node<T>*;

	//if (!iteratorptr)
	//{
	//	std::cout << "[]";
	//	return;
	//}
	//while (iteratorptr)
	//{
	//	cout << "["
	//		 << iteratorptr->getItem() << ",";
	//	iteratorptr = iteratorptr->getNext();
	//}
	//std::cout << "]";
	//return;
}
template<typename T>
void DerivedQueue<T>::cancelRequest()
{
	/// Ph2 isa
}